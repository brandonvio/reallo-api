version: 0.2

phases:
  install:
    commands:
      - npm i npm@latest -g
      - pip install --upgrade pip
      - pip install --upgrade awscli
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - echo building... attempt 4...
  post_build:
    commands:
      - aws cloudformation package --template-file template.yaml --output-template-file serverless-output.yaml --s3-bucket brandonvio-re-demo-api
      - aws cloudformation deploy --template-file serverless-output.yaml --stack-name redemo --capabilities CAPABILITY_IAM
